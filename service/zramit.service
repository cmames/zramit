[Unit]
Description=zram swap service
Requires=systemd-modules-load.service
Before=shutdown.target
#After=

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/zramit-script.sh start
ExecStop=/usr/local/sbin/zramit-script.sh stop
RemainAfterExit=True

[Install]
WantedBy=sysinit.target
#RequiredBy=
